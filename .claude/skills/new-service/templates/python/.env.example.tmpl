# __SERVICE__ settings for running on the host (`make run`, tests). Copy to .env (never
# committed). In containers, compose sets API_HOST/API_PORT in `environment:`.
# Host ports are NOT set here: see the root .env.example (__ENV_PREFIX___HTTP_PORT).

LOG_LEVEL=INFO

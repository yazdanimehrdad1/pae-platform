// Runtime config for `npm run dev` / `vite preview`. In the container this file is
// overwritten at start from the environment (docker/40-app-config.sh). Read in the app
// through src/shared/config/runtime.ts only.
window.__APP_CONFIG__ = {
  apiBaseUrl: "/api",
};
